# [Plugin Name]

**What it is (plain English):**

**When we reach for it:**

**Key controls:** (name → what it does, in human words)

**Macro rack:** AGT [name] — knob list with names

**Agent access:** macro rack / direct params (see `.params.json`) / manual only

**Gotchas:**

**Recipes that use it:** (links into /skills)

---

*To generate this plugin's `.params.json`, load it on a track in Ableton and
run:*

```bash
python ableton-mcp/tools/param_dump.py --track <t> --device <d> --name <plugin-slug>
```

*Rule: document plugins the day you first reach for them, not all upfront.*
