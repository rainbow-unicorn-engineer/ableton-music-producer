# Library Analyst dependencies
# Install:  pip install -r requirements.txt
mcp[cli]>=1.3.0,<2
numpy>=1.24
librosa>=0.10          # the analysis engine the plan calls for
soundfile>=0.12        # audio decoding for librosa
# Optional power-up, install later per the plan ("the cherry, not the cake"):
# laion-clap            # true vibe search via audio embeddings
