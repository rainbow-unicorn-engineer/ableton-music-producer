# [Track / Style Name] — recipe

*One recipe = one dissected reference, written so you never have to
figure it out twice. Fill this in during/after a dissection session.
Everything a stranger would need to cook this style from your kitchen.*

**Reference card:** `references/<name>.reference.json` (key, BPM, LUFS,
band balance — the numbers live there; don't copy them, link them)

**The sound in one sentence:** (e.g. "funk pocket at 144 with a talky
mid bass under a rap vocal, warm top, sub never stops")

## Arrangement math

From the card's skeleton, plus what the energy map doesn't say:
- Section lengths and order (intro 8 → drop 20 → ...)
- Where the hook first appears, and how often it returns
- What changes at each section boundary (the "why does it stay
  interesting" column)

## Sound palette

One line per major element: what it is, and the `find_sounds` /
`vibe_search` query that fetches your closest match.
- Kick: (e.g. "punchy kick near G" → <chosen sample path>)
- Sub:
- Mid bass / growl:
- Drums/percussion:
- Vocal treatment:
- Texture/FX:

## Signature techniques

Step-by-step Ableton moves for the 2-3 tricks that MAKE this style.
Concrete: device, knob, amount. (e.g. "Serum growl: WT 'Talking', warp
FM from OSC B 30%, notch filter + LFO 1 on cutoff 1/4 rate; Saturn 2
after, mids-only drive 4; ShaperBox volume dip on every kick")

## Mix targets

- Loudness / true peak / dynamics: (from the card — restate the goal,
  e.g. "-6 LUFS crushed, but keep 12 LU range via quiet bookends")
- Band balance notes: (e.g. "sub and bass equal, mids forward for vocal,
  highs rolled off — do NOT brighten")
- Per-stem balance (from separate_stems): (e.g. "drums 2 LU under bass")
- Clash rules (from analyze_clash): (e.g. "duck 250-500 in beat when
  vocal plays")

## What I learned

Honest notes after using this recipe: what worked, what needed changing,
what to try next time. This section is why the cookbook gets smarter.
